query ExerciseTimeline($exerciseId: ID!) {
  me {
    maxPulse
  }

  exercise(id: $exerciseId) {
    id
    startDay {
      start
      heartRateSummary {
        samples {
          time
          heartRate
        }
      }
    }
  }
}
