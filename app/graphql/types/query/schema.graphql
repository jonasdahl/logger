type Query {
  me: User
  activities(
    first: Int
    after: String
    last: Int
    before: String
    filter: ActivityFilter
  ): ActivityConnection!
}

input ActivityFilter {
  startFrom: DateTime
  startTo: DateTime
}
